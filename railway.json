{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npx --yes serve dist -l $PORT -s --no-request-logging",
    "restartPolicyType": "ON_FAILURE"
  }
}
