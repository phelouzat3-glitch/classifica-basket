{
  "$schema": "https://railway.app",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx --yes serve dist -l $PORT --single",
    "restartPolicyType": "ON_FAILURE"
  }
}
